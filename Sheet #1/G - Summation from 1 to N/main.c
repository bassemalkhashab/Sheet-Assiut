#include <stdio.h>

int main()
{
    // The equation used to calculate the summation of all numbers to the number N is : N(N+1)/2
    long long int x;
    scanf("%lld",&x);
    printf("%lld",x*(x+1)/2);
    return 0;
}
